variables:
  TAG_COMMIT: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_NAME:$CI_COMMIT_SHORT_SHA

image: openjdk17

deploy:
  stage: deploy
  tags: 
    - spring_modules
  before_script:
    - apt update && apt -y install make && apt -y install maven
  script:
    - uname -a
    - whoami
    - make up
